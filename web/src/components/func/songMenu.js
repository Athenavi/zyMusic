let musicId = localStorage.getItem('currentId') || null;
export const likeThisSong = () => {
    alert(`收藏还未实现添加喜欢成功${musicId}`);
};

export const shareThisSong = () => {
    alert(`分享还未实现${musicId}`);
};
const downloadThisSong = () => {
    alert('下载任务已开启');
};
